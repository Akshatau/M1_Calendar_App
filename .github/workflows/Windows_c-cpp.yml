name: Windows C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: powershell

    steps:
    - uses: actions/checkout@v2
    - name: build
    - run: make Run 
    
    
